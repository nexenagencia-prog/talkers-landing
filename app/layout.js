import './globals.css';

export const metadata = {
  title: 'Premium Landing CMS',
  description: 'Landing page premium com CMS completo'
};

export default function RootLayout({ children }) {
  return <html lang="pt-BR"><body>{children}</body></html>;
}
